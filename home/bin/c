#!/bin/bash

out=a.out

for arg in "$@"; do
	if [[ $arg == *.cpp ]]; then
		out=${arg%.cpp}
		break
	fi
	if [[ $arg == *.c ]]; then
		out=${arg%.c}
		break
	fi
done

exec g++ -Wall -Wconversion -Wfatal-errors -g -std=c++17 -fsanitize=undefined,address -o "$out" "$@"
